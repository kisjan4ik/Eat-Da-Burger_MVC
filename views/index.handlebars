
<div class="container">
<h1><img src="/assets/css/images/hamburger.png" alt="Burger">
<br>
Eat-Da-Burger!</h1>

<div class="row">
  <div class="col-sm-6 menu">
<h2>Choose what you want to eat:</h2>

<ul>
  {{#each burgers}}
    {{#unless this.devour}}
      {{> burgers/burger-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>
</div>
<div class="col-sm-6">
<h2 id="devoured">Devoured:</h2>

<ul>
  {{#each burgers}}
    {{#if devour}}
      {{> burgers/burger-block devour=false}}
    {{/if}}
  {{/each}}
</ul>
</div>
</div>

<h3>Burger Menu:</h3>
<form class="create-form">

  <div class="form-group">
    
    <textarea class="form-control" id="burger" name="name" rows="2"></textarea>
  </div>

  <div class="form-group">
   
    <input type="radio" name="devour" value="0" checked> Enter a burger you want to eat and click "Add" button!<br>
      <button type="submit" class="btn btn-primary" id="add">Add </button>
  </div>


</form>
</div>